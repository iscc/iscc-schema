title: iscc-generator
type: object
description: Inputs for ISCC generation
properties:
  source_file:
    description: The media file used as source for ISCC generation.
    type: string
    format: binary
    writeOnly: true
  source_url:
    description: URL of a the media file used as source for ISCC generation.
    type: string
    format: uri
    writeOnly: true
